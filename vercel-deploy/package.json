{
  "name": "admissions-atlas-vercel",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build:frontend": "cd .. && npm run build && cp -r dist/* vercel-deploy/public/",
    "deploy": "vercel deploy --prod"
  },
  "dependencies": {
    "@google/genai": "^2.4.0",
    "@supabase/supabase-js": "^2.107.0",
    "cors": "^2.8.6",
    "express": "^4.21.2",
    "express-rate-limit": "^8.5.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.25",
    "@types/node": "^22.19.20",
    "typescript": "~5.8.2"
  }
}
